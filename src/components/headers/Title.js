import React from "react";

function title() {
  const heading = {
    fontFamily: "Dancing Script",
    fontSize: "88px",
    fontWeight: "normal",
  };
  return (
    <div className="row" style={heading}>
      lotus flow
    </div>
  );
}

export default title;
